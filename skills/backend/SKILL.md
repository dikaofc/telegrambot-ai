# backend skill
description: backend/API implementation workflow

instructions: |
  design endpoints with validation (zod)
  protect against SSRF/injection
  add tests for new routes
  verify with test/build
