# git skill
description: git workflow for agents

instructions: |
  check git status and diff before changing
  commit in small logical units with clear messages
  never push without approval
  never force-push without explicit approval
